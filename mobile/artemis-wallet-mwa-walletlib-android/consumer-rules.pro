# Reflection is not used by the walletlib runtime. The protocol package
# in :artemis-wallet-mwa-android already declares its own consumer rules;
# this file exists so the AGP packaging task does not warn about a
# missing referenced ProGuard file.
