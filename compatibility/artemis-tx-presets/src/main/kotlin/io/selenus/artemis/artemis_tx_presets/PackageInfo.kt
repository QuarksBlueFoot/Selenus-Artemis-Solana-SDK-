package io.selenus.artemis.artemis_tx_presets

/**
 * artemis-tx-presets
 *
 * Optional, mobile-first transaction composer presets.
 */
object PackageInfo {
  const val name: String = "artemis-tx-presets"
}
