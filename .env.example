# Environment Variables Template
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# Devnet wallet seed (Base58 encoded)
# Generate with: solana-keygen new --no-bip39-passphrase
DEVNET_WALLET_SEED=

# RPC endpoints (optional - defaults to public devnet)
DEVNET_RPC_URL=https://api.devnet.solana.com
DEVNET_WS_URL=wss://api.devnet.solana.com

# For faster RPC, use a provider like Helius:
# DEVNET_RPC_URL=https://devnet.helius-rpc.com/?api-key=YOUR_API_KEY
# DEVNET_WS_URL=wss://devnet.helius-rpc.com/?api-key=YOUR_API_KEY
